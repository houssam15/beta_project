import 'package:alpha_flutter_project/login/login.dart';

class QrcodeItemLink{
  String url;
  IconData icon;
  String label;

  QrcodeItemLink({
    required this.url,
    required this.icon,
    required this.label
  });

}